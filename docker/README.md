# Docker (placeholder)

Built in Phase 9 of `IMPLEMENTATION_PLAN.md`. Will contain:

- `docker-compose.yml` — `caddy`, `backend`, `piper-tts` services
- `Caddyfile` — auto-HTTPS reverse proxy template
- `Dockerfile.backend`, `Dockerfile.frontend`, `Dockerfile.piper`
- `docker-compose.override.yml` — local dev overrides
- `DEPLOYMENT.md` — env vars, ports, healthchecks

These are reference artifacts only — user handles deployment manually.
