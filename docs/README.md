# Docs

Notes, mapping templates, and reference material that supports the implementation.

Planned contents:

- `blendshape-mapping-template.json` — Oculus viseme → custom blendshape name mapping (filled in Phase 4 after the user provides the spreadsheet)
- `phoneme-to-viseme.md` — espeak IPA → Oculus viseme reference table (built in Phase 4)
- `latency-budget.md` — per-stage timing targets used in Phase 11 tuning
